<table class="table table-hover">
  <tbody>
    <tr>
      <th>{{comment.user.email}}</th>
      <th>{{comment.user.email}}</th>
      <th>{{comment.user.email}}</th>
      <th><a href="{{comment.commentable}}/{{comment.commentableId}}
        delete_comment?comment_id={{comment.id}}" data-remote="true",
        data-method="delete" rel="nofollow"></a>remove</th>
    </tr>
  </tbody>
</table>
          