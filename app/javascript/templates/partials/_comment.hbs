<div class="comment" id="{{comment.id}}" data-commentable-id="{{comment.commentable_id}}">
  <table class="table table-hover">
    <tbody>
      <tr>
        <th class="d-flex flex-row">
          <div class="connent-created-at me-2">{{comment.updated_at}}</div>
          <div class="comment-author text-primary me-2">{{comment.users_email}}</div>
          <div class="comment-body">{{comment.body}}</div>
        </th>
        {{!-- {{#if comment.user_id}} --}}
        {{!-- <th> --}}
          {{!-- <a class="remove-comment-link" data-id="{{comment.id}}" 
            data-remote="true" 
            rel="nofollow"
            data-method="delete" 
            href="/{{comment.commentable}}/{{comment.commentable_id}}/delete_comment?comment_id={{comment.id}}">remove</a></th> --}}
      </tr>
    </tbody>
  </table>
</div>