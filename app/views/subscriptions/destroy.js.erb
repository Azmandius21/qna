$('.subscription-errors').html("<%= render 'shared/errors', resource: @subscription %>");

<% if !@subscription.errors.present? %>
  $('div#question-<%= @subscription.question %>.question-subscription').
  .html('<%= j render 'shared/subscription', resource: @subscription.question, subscription: nil  %>');
<% end %>