$('.answer-errors').html("<%= render 'shared/errors', resource: @answer %>");

<% if !@answer.errors.present? %>
  $('.best-answer').html('<%= j render @best_answer %>');
  $('.other-answers').html('<%= j render @other_answers %>');
<% end %>